const initialState = {
  pictures: [],
  loading: false,
  failed: false,
};

export const reducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
